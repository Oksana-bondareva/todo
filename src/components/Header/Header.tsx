import { AppBar, Typography } from "@mui/material";

const Header = () => {
    return (
        <>
            <AppBar>
                <Typography variant="h3">
                    ToDo List
                </Typography>
            </AppBar>
        </>
    )
}

export default Header;